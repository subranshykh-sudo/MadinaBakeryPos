<UserControl x:Class="MadinaBakeryPos.Views.SidebarView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MadinaBakeryPos.Views"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        mc:Ignorable="d" 
        Width="Auto" 
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls">
    <Border Style="{StaticResource SidebarContainerStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- TOP MENU -->
            <StackPanel Grid.Row="0">

                <!-- Dashboard -->
                <Button Style="{StaticResource SidebarButtonStyle}" Command="{Binding ShowDashboard}">
                    <Grid Margin="12" VerticalAlignment="Center">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="24"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <iconPacks:PackIconMaterial
                Grid.Column="0"
                Kind="ViewDashboardOutline"
                Width="20"
                Height="20"
                VerticalAlignment="Center"
                HorizontalAlignment="Center"/>

                        <TextBlock
                Grid.Column="1"
                Text="Dashboard"
                VerticalAlignment="Center"
                Margin="10,0,0,0"/>
                    </Grid>
                </Button>

                <!-- Inventory -->
                <Button Style="{StaticResource SidebarButtonStyle}" Command="{Binding ShowInventory}">
                    <Grid Margin="12" VerticalAlignment="Center">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="24"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <iconPacks:PackIconMaterial
                Grid.Column="0"
                Kind="PackageVariantClosed"
                Width="20"
                Height="20"
                VerticalAlignment="Center"
                HorizontalAlignment="Center"/>

                        <TextBlock Grid.Column="1" Text="Inventory" VerticalAlignment="Center" Margin="10,0,0,0"/>
                    </Grid>
                </Button>

                <!-- Products -->
                <Button Style="{StaticResource SidebarButtonStyle}" Command="{Binding ShowProduct}">
                    <Grid Margin="12" VerticalAlignment="Center">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="24"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <iconPacks:PackIconMaterial Grid.Column="0" Kind="Package" Width="20" Height="20"
                                        VerticalAlignment="Center" HorizontalAlignment="Center"/>

                        <TextBlock Grid.Column="1" Text="Products" VerticalAlignment="Center" Margin="10,0,0,0"/>
                    </Grid>
                </Button>

                <!-- Stocks -->
                <Button Style="{StaticResource SidebarButtonStyle}" Command="{Binding ShowStock}">
                    <Grid Margin="12" VerticalAlignment="Center">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="24"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <iconPacks:PackIconMaterial Grid.Column="0" Kind="ArchiveOutline" Width="20" Height="20"
                                        VerticalAlignment="Center" HorizontalAlignment="Center"/>

                        <TextBlock Grid.Column="1" Text="Stocks" VerticalAlignment="Center" Margin="10,0,0,0"/>
                    </Grid>
                </Button>

                <!-- Stats -->
                <Button Style="{StaticResource SidebarButtonStyle}" Command="{Binding ShowStats}">
                    <Grid Margin="12" VerticalAlignment="Center">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="24"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <iconPacks:PackIconMaterial Grid.Column="0" Kind="ChartLine" Width="20" Height="20"
                                        VerticalAlignment="Center" HorizontalAlignment="Center"/>

                        <TextBlock Grid.Column="1" Text="Stats" VerticalAlignment="Center" Margin="10,0,0,0"/>
                    </Grid>
                </Button>

                <!-- Expense -->
                <Button Style="{StaticResource SidebarButtonStyle}" Command="{Binding ShowExpense}">
                    <Grid Margin="12" VerticalAlignment="Center">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="24"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <iconPacks:PackIconMaterial Grid.Column="0" Kind="CashMinus" Width="20" Height="20"
                                        VerticalAlignment="Center" HorizontalAlignment="Center"/>

                        <TextBlock Grid.Column="1" Text="Expense" VerticalAlignment="Center" Margin="10,0,0,0"/>
                    </Grid>
                </Button>

                <!-- Audit -->
                <Button Style="{StaticResource SidebarButtonStyle}" Command="{Binding ShowAudith}">
                    <Grid Margin="12" VerticalAlignment="Center">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="24"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <iconPacks:PackIconMaterial Grid.Column="0" Kind="FileDocumentCheckOutline" Width="20" Height="20"
                                        VerticalAlignment="Center" HorizontalAlignment="Center"/>

                        <TextBlock Grid.Column="1" Text="Audit" VerticalAlignment="Center" Margin="10,0,0,0"/>
                    </Grid>
                </Button>

            </StackPanel>

            <!-- Bottom Settings -->
            <Button Grid.Row="1"
        Style="{StaticResource SidebarButtonStyle}"
        VerticalAlignment="Bottom" Command="{Binding ShowSettings}">
                <Grid Margin="12" VerticalAlignment="Center">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="24"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <iconPacks:PackIconMaterial Grid.Column="0" Kind="CogOutline" Width="20" Height="20"
                                    VerticalAlignment="Center" HorizontalAlignment="Center"/>

                    <TextBlock Grid.Column="1" Text="Settings" VerticalAlignment="Center" Margin="10,0,0,0"/>
                </Grid>
            </Button>


        </Grid>
    </Border>
</UserControl>
